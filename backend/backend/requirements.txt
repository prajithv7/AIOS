# Core requirements (Python 3.12+). Pins are minimums so pip resolves
# 3.14-compatible wheels. LiteLLM is optional (see requirements-lite.txt).
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0
email-validator>=2.2
sqlalchemy>=2.0.36
aiosqlite>=0.20
python-jose[cryptography]>=3.3
bcrypt>=4.2
cryptography>=44.0
httpx>=0.28
redis>=5.2
pytest>=8.3
pytest-asyncio>=0.25

# Optional: unified LLM gateway (LiteLLM). Requires Python <3.14.
# Install with: pip install -r requirements-lite.txt
